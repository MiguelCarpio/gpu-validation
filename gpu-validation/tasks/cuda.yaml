---
- name: Install torch libs
  ansible.builtin.pip:
    name: torch
    state: present
    extra_args: -i {{ gpu_validation_pytorch_url }}

- name: Install numpy libs
  ansible.builtin.pip:
    name: numpy
    state: present
